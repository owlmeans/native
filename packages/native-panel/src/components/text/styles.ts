import { StyleSheet } from 'react-native'

export const TextValueStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    minHeight: 50
  },
  value: {
  },
  actions: {
  }
})
